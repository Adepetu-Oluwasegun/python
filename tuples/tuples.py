# its a container we can store different values. its different from lists. with some key differences, tuples are immutable(they can't be changed or modified) unlike lists. elements in a tuple are easilt accesible

coordinates = (4, 5)
print(coordinates[1])

# a list of tuples
coordinates = [(4,5), (7,6), (9,3)]